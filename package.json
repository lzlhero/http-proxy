{
  "name": "http-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx pm2 start index.js --name=http-proxy -l http-proxy.log -o /dev/null -e /dev/null --merge-logs",
    "stop": "npx pm2 delete http-proxy",
    "restart": "npx pm2 restart http-proxy",
    "debug": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socks": "^1.1.10"
  },
  "devDependencies": {
    "pm2": "^2.8.0"
  }
}
