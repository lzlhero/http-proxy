{
  "name": "http-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "http-proxy.js",
  "scripts": {
    "start": "pm2 start http-proxy.js --name=http-proxy -l http-proxy.log -o /dev/null -e /dev/null --merge-logs",
    "stop": "pm2 delete http-proxy",
    "reload": "pm2 restart http-proxy",
    "load": "node http-proxy.js > /dev/null &",
    "kill": "pkill -9 -f http-proxy.js",
    "debug": "node http-proxy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socks": "^1.1.10",
    "stream-replace": "^1.0.0"
  },
  "devDependencies": {
    "pm2": "^3.5.1"
  }
}
