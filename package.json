{
  "name": "http-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pm2 start index.js --name=http-proxy -l http-proxy.log -o /dev/null -e /dev/null --merge-logs",
    "stop": "pm2 delete http-proxy",
    "restart": "pm2 restart http-proxy",
    "load": "node index.js > /dev/null &",
    "kill": "pkill -9 -f index.js",
    "debug": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socks": "^1.1.10",
    "stream-replace": "^1.0.0"
  },
  "devDependencies": {
    "pm2": "^2.8.0"
  }
}
